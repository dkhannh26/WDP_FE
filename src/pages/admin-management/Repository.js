import React from "react";
import { Outlet } from "react-router-dom";

const Repository = () => {
    return (
        <>
            <Outlet></Outlet>
        </>
    );
};

export default Repository;

import React from 'react';
import { Outlet } from 'react-router-dom';

const Permission = () => {
    return (
        <>
            <Outlet></Outlet>
        </>
    );
};

export default Permission;
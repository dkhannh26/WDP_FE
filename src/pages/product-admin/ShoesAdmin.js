import React from 'react';
import { Outlet } from 'react-router-dom';

const ShoesAdmin = () => {
    return (
        <>
            <Outlet></Outlet>
        </>
    );
};

export default ShoesAdmin;
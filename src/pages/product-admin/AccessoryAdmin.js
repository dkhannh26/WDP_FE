import React from 'react';
import { Outlet } from 'react-router-dom';

const AccessoryAdmin = () => {
    return (
        <>
            <Outlet></Outlet>
        </>
    );
};

export default AccessoryAdmin;
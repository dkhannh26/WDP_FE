import React from 'react';
import { Outlet } from 'react-router-dom';

const RacketAdmin = () => {
    return (
        <>
            <Outlet></Outlet>
        </>
    );
};

export default RacketAdmin;
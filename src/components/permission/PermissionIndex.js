import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const PermissionIndex = () => {
    return (
        <div>
            <Title level={2}>Permission for role</Title>
        </div>
    );
};

export default PermissionIndex;